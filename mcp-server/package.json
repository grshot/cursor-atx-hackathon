{
  "name": "scout-mcp-server",
  "version": "0.0.0",
  "private": true,
  "description": "MCP server exposing Scout's scout_search tool. See Phase 5.",
  "type": "module",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --alias:@=..",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "esbuild": "^0.28.2",
    "zod": "^4.4.3"
  }
}
