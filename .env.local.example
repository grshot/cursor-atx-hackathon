# xAI Responses API key (Grok 4.6, web_search, x_search) — required
GROK_API_KEY=

# Semantic Scholar API key — required for Phase 4's academic agent.
# Anonymous access works for single-paper lookups but is rate-limited
# on /paper/search; get a free key at https://www.semanticscholar.org/product/api
SEMANTIC_SCHOLAR_API_KEY=
